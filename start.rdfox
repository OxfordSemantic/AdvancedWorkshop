# Output RDFox's response to the terminal.
set output out

# Start the endpoint (to enable API calls).
endpoint start

# Create a data store called 'default' - this will be the default graph.
dstore create default
