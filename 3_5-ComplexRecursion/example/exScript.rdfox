# Clear the datastore to ensure we start with a blank slate
clear force

# Deactivate the reasoning profiler and the reasoning plans, setting the frequency to 0 seconds
set reason.monitor off
set reason.profiler.log-plans false
set log-frequency 0

# Import our data
import +p 3_5-ComplexRecursion/example/exData.ttl
